<view class="container">
  <scroll-view class='imgBox' scroll-y="true" bindscrolltolower="lower" style="height:2000px;">
    <image wx:for="{{list}}" wx:key="{{item.createdAt}}" class='img' src='{{item.url}}' mode='widthFix'></image>
  </scroll-view>
  <view>
    
  </view>
</view>